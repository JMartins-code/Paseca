{% extends 'menu.html.twig' %}

{% block title %}Descriptions
{% endblock %}

{% block body %}
	<h1 class="card-title ">Description d'une archive</h1>

	<table class="table">
		<tbody>
			<tr>
				<th class="text-left">Code</th>
				<td>{{ description.codeDescription }}</td>
			</tr>
			<tr>
				<th class="text-left">Etat matériel</th>
				<td>{{ description.etatMateriel }}</td>
			</tr>
			<tr>
				<th class="text-left">Nature</th>
				<td>{{ description.natureArchive }}</td>
			</tr>
		</tbody>
	</table>

	<div class="template-demo">
		<a href="{{ path('descriptions_index') }}">
			<img src="{{ asset('../../assets/images/back.png') }}" alt="user" height="30px"/>
		</a>
		<a href="{{ path('descriptions_edit', {'id': description.id}) }}" class="mdc-button mdc-button--outlined outlined-button--info mdc-ripple-upgraded">
			<i class="material-icons mdc-button__icon">edit</i>
			Modifier
		</a>
		{{ include('descriptions/_delete_form.html.twig') }}
	</div>
{% endblock %}
