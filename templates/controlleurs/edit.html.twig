{% extends 'menu.html.twig' %}

{% block title %}Modifier controleur
{% endblock %}

{% block body %}
	<h1 class="card-title card-padding pb-0">Modifier controleur</h1>

	{{ include('controlleurs/_form.html.twig', {'button_label': 'Modifier'}) }}

	<div class="menu-button-container">
		<a href="{{ path('controlleurs_index') }}">
			<img src="{{ asset('../../assets/images/back.png') }}" alt="user" height="30px"/>
		</a>
	</div>

	{{ include('controlleurs/_delete_form.html.twig') }}
{% endblock %}
