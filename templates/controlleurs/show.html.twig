{% extends 'menu.html.twig' %}

{% block title %}Controleurs
{% endblock %}

{% block body %}
	<h1 class="card-title card-padding pb-0">Controleurs</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Code</th>
				<td>{{ controlleur.codeControlleur }}</td>
			</tr>
			<tr>
				<th>Code utilisateur</th>
				<td>{{ controlleur.codeUtilisateur }}</td>
			</tr>
		</tbody>
	</table>

	<div class="template-demo">
		<a href="{{ path('controlleurs_index') }}">
			<img src="{{ asset('../../assets/images/back.png') }}" alt="user" height="30px"/>
		</a>
		<a href="{{ path('controlleurs_edit', {'id': controlleur.id}) }}" class="mdc-button mdc-button--outlined outlined-button--info mdc-ripple-upgraded">
			<i class="material-icons mdc-button__icon">edit</i>
			Modifier
		</a>
	</div>
	{{ include('controlleurs/_delete_form.html.twig') }}
{% endblock %}
