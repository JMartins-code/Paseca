{% extends 'menu.html.twig' %}

{% block title %}Modifier transferts
{% endblock %}

{% block body %}
	<h1 class="card-title card-padding pb-0">Modifier transferts</h1>

	{{ include('transferts/_form.html.twig', {'button_label': 'Modifier'}) }}

	<div class="menu-button-container">
		<a href="{{ path('transferts_index') }}">
			<img src="{{ asset('../../assets/images/back.png') }}" alt="user" height="30px"/>
		</a>
	</div>
	{{ include('transferts/_delete_form.html.twig') }}
{% endblock %}
