{% extends 'menu.html.twig' %}

{% block title %}Modifier l'archive
{% endblock %}

{% block body %}
	<h1 class="card-title card-padding pb-0">Modification de l'archive</h1>

	{{ include('archives/_form.html.twig', {'button_label': 'Modifier'}) }}

	<div class="menu-button-container">
		<a href="{{ path('archives_index') }}">
			<img src="{{ asset('../../assets/images/back.png') }}" alt="user" height="30px"/>
		</a>
	</div>
	{{ include('archives/_delete_form.html.twig') }}
{% endblock %}
