{% extends 'menu.html.twig' %}

{% block title %}Modifier institution
{% endblock %}

{% block body %}
	<h1 class="card-title card-padding pb-0">Modifier l'nstitutions</h1>

	{{ include('institutions/_form.html.twig', {'button_label': 'Modifier'}) }}

	<div class="menu-button-container">
		<a href="{{ path('institutions_index') }}">
			<img src="{{ asset('../../assets/images/back.png') }}" alt="user" height="30px"/>
		</a>
	</div>
	{{ include('institutions/_delete_form.html.twig') }}
{% endblock %}
