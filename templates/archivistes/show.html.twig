{% extends 'menu.html.twig' %}

{% block title %}Archivistes
{% endblock %}

{% block body %}
	<h1 class="card-title card-padding pb-0">Archiviste</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Code</th>
				<td>{{ archiviste.codeArchiviste }}</td>
			</tr>
			<tr>
				<th>Code utilisateur</th>
				<td>{{ archiviste.codeUtilisateur.nom }}</td>
			</tr>
		</tbody>
	</table>

	<div class="template-demo">
		<a href="{{ path('archivistes_index') }}">
			<img src="{{ asset('../../assets/images/back.png') }}" alt="user" height="30px"/>
		</a>
		<a href="{{ path('archivistes_edit', {'id': archiviste.id}) }}" class="mdc-button mdc-button--outlined outlined-button--info mdc-ripple-upgraded">
			<i class="material-icons mdc-button__icon">edit</i>
			Modifier
		</a>
	</div>
	{{ include('archivistes/_delete_form.html.twig') }}
{% endblock %}
